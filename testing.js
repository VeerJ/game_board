// initialize array
let arr = [

];
let a = new Array();

// append new value to the array
// arr.push("{'test' : 2}");
// a= arr[1].split(",");
// console.log(a);

for (let i=0;i<3;i++){
    console.log(arr[i]);
    a = arr[i].split(",");
    console.log(a);
    f_value = a[0];
    console.log("i ki value is" + f_value[0]);
}

let i=0;
let j=1;

updateVal(i,j)
    let ans = (i.toString() + "," + j.toString(); 
    return ans;
}

